import React from 'react';
import ReactDOM from 'react-dom/client';
import QuizAppCompleto from './QuizAppCompleto';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <QuizAppCompleto />
  </React.StrictMode>
);
